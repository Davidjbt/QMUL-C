#include <stdio.h>
#include "shapes.h"

char character = '.'; // global variable, which will use in the function inside the shapes header file.
// main function, the program will start from this function.
void main() {
    drawParallelogram(4); // call to method (inside shapes.h)
}